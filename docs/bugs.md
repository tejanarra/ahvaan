1. very tricky to drag and drop to nested components lot bouncier and not easy keep snapping, if there are multiple nested components very difficult to drag and drop between them 

2. irregular dotted lines sizes for contianers 

3. very difficult to understand and modify sizing and layout for a normal user who has no experience with development.

4. ability to add og-image for every event page. if not provide a default one by ourself.

5. logo not matching from icon.svg
